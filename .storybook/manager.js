// .storybook/manager.js

import { addons } from '@storybook/manager-api';
// import { themes } from '@storybook/theming';
import stingTheme from './stingTheme';

addons.setConfig({
    // theme: themes.dark,
    // theme: stingTheme
});