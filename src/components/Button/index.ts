// Export the Button component and its related types
export { Button as default, Button, type ButtonComponent } from "./Button";
export type {
  ButtonProps,
  ButtonVariant,
  ButtonSize,
  ButtonType,
  BuilderInputConfig,
} from "./Button.types";

// Re-export for convenience
export * from "./Button.types";
